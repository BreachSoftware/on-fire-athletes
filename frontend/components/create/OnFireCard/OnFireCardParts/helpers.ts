// Use this enum to determine the zIndex of the elements on the card
export enum zIndex {
    background = 1,
    petch = 2,
    hero = 3,
    signature = 4,
    text = 5,
    border = 6,
    cardBackVideo = 7,
}
